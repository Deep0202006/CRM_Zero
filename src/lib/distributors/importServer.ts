import {createHash} from "crypto";import type {SupabaseClient} from "@supabase/supabase-js";import {normalizedIdentity} from "./domain";import type {DistributorImportRow} from "./import";import {requestHash} from "./server";
export type DistributorImportClassification="NEW"|"UPDATE"|"EXACT_DUPLICATE"|"AMBIGUOUS"|"INVALID_EMPLOYEE";
function stableId(operationId:string,rowNumber:number){const hex=createHash("sha256").update(`${operationId}:${rowNumber}`).digest("hex");return `${hex.slice(0,8)}-${hex.slice(8,12)}-4${hex.slice(13,16)}-a${hex.slice(17,20)}-${hex.slice(20,32)}`}
const critical=(value:Record<string,unknown>)=>requestHash({distributor_name:value.distributor_name,distributor_reference:value.distributor_reference??"",assigned_to:value.assigned_to,installation_status:value.installation_status,installation_completed_at:value.installation_completed_at??null,training_status:value.training_status,training_completed_at:value.training_completed_at??null,activity_status:value.activity_status,billing_status:value.billing_status,billed_at:value.billed_at??null,bill_reference:value.bill_reference??"",renewal_date:value.renewal_date??null});
export async function buildDistributorImportPreview(service:SupabaseClient,operationId:string,rows:DistributorImportRow[]){const [{data:users},{data:adminCaps}]=await Promise.all([service.from("users").select("user_id,name,email").eq("is_active",true),service.from("user_capabilities").select("user_id").eq("capability_code","admin")]);const admins=new Set((adminCaps??[]).map(row=>row.user_id)),employees=new Map((users??[]).filter(user=>!admins.has(user.user_id)).map(user=>[String(user.email).toLowerCase(),user]));const keys=[...new Set(rows.map(row=>normalizedIdentity(row.distributorName,row.distributorReference)))],existing:Record<string,unknown>[]=[];for(let index=0;index<keys.length;index+=100){const {data,error}=await service.from("distributor_accounts").select("distributor_id,identity_key,distributor_name,distributor_reference,assigned_to,installation_status,installation_completed_at,training_status,training_completed_at,activity_status,billing_status,billed_at,bill_reference,renewal_date,version").in("identity_key",keys.slice(index,index+100));if(error)throw error;existing.push(...(data??[]))}const byKey=new Map<string,Record<string,unknown>[]>();existing.forEach(record=>byKey.set(String(record.identity_key),[...(byKey.get(String(record.identity_key))??[]),record]));const seen=new Set<string>(),classified=rows.map(row=>{const key=normalizedIdentity(row.distributorName,row.distributorReference),employee=employees.get(row.assignedEmployeeEmail),matches=byKey.get(key)??[];if(!employee)return {...row,classification:"INVALID_EMPLOYEE" as const,reason:"Assigned employee is missing, inactive, or an Admin."};if(seen.has(key))return {...row,classification:"AMBIGUOUS" as const,reason:"This file repeats a distributor identity."};seen.add(key);if(matches.length>1)return {...row,classification:"AMBIGUOUS" as const,reason:"Multiple distributors match this name. Add a stable Distributor Reference."};const current=matches[0],payload={distributor_id:current?.distributor_id??stableId(operationId,row.rowNumber),expected_version:current?.version,distributor_name:row.distributorName,distributor_reference:row.distributorReference,lead_id:null,phone:"",city:"",assigned_to:employee.user_id,installation_status:row.installationStatus,installation_completed_at:row.installationDate||null,training_status:row.trainingStatus,training_completed_at:row.trainingDate||null,activity_status:row.activityStatus,billing_status:row.billingStatus,billed_at:row.billDate||null,bill_reference:row.billReference,renewal_date:row.renewalDate||null,note:"Imported from spreadsheet",identity_key:key};const comparison=current&&critical(payload)===critical(current);return {...row,classification:(comparison?"EXACT_DUPLICATE":current?"UPDATE":"NEW") as DistributorImportClassification,assigned_employee_name:employee.name,payload}});const counts=classified.reduce<Record<string,number>>((result,row)=>(result[row.classification]=(result[row.classification]??0)+1,result),{}),preview_hash=requestHash(classified.map(row=>({rowNumber:row.rowNumber,classification:row.classification,payload:"payload" in row?row.payload:null})));return {rows:classified,counts,preview_hash}}
