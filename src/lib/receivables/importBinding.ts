import type { ImportRow } from "./import";
export interface ImportBindingRow extends ImportRow {classification:string;business_key?:string;assigned_to?:string;receivable_id?:string}
export function importPreviewBinding(classified:ImportBindingRow[],resolvedRows:Record<string,unknown>[]){return {canonicalClassifications:classified.map(row=>({row_number:row.rowNumber,classification:row.classification,business_identity:row.business_key??null,receivable_id:row.receivable_id??null,bill_reference:row.billReference,distributor_name:row.distributorName,distributor_code:row.distributorCode,contact_person:row.contactPerson,contact_phone:row.contactPhone,bill_amount:row.billAmount,bill_due_date:row.billDueDate,next_follow_up_date:row.nextFollowUpDate,assigned_to:row.assigned_to??null,notes:row.notes})),canonicalResolvedRows:resolvedRows}}
