import {addISTDateDays} from "@/lib/dateTime";import {renewalState,validateStatusCombination} from "@/lib/distributors/domain";
describe("Distributor Status domain",()=>{test.each([["2026-08-16","renewal_upcoming"],["2026-08-15","renewal_due_in_2_days"],["2026-08-14","renewal_due_tomorrow"],["2026-08-13","renewal_due_today"],["2026-08-12","renewal_overdue"],[null,"none"]])("derives IST renewal state for %s",(date,state)=>expect(renewalState(date,"2026-08-13")).toBe(state));test("adds IST calendar days across month/year boundaries",()=>{expect(addISTDateDays("2026-12-31",1)).toBe("2027-01-01");expect(addISTDateDays("2026-02-28",1)).toBe("2026-03-01")});test("rejects invalid orthogonal state combinations",()=>{expect(validateStatusCombination({installation_status:"pending",training_status:"done",activity_status:"not_applicable"})).toMatch(/Training/);expect(validateStatusCombination({installation_status:"done",training_status:"pending",activity_status:"active"})).toMatch(/Activity/);expect(validateStatusCombination({installation_status:"done",training_status:"done",activity_status:"active"})).toBeNull();expect(validateStatusCombination({installation_status:"done",training_status:"done",activity_status:"inactive"})).toBeNull()})});
