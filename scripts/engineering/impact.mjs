import { execFileSync } from "node:child_process";import { createHash } from "node:crypto";import { readFileSync } from "node:fs";import { resolve } from "node:path";
const root=resolve(import.meta.dirname,"../.."),args=process.argv.slice(2),value=k=>args[args.indexOf(k)+1],base=value("--base")??"origin/main",head=value("--head")??"HEAD",git=(...a)=>execFileSync("git",a,{cwd:root,encoding:"utf8"}).trim();const paths=git("diff","--name-only",`${base}...${head}`).split(/\r?\n/).filter(Boolean),patch=git("diff","--unified=0",`${base}...${head}`),map=JSON.parse(readFileSync(resolve(root,"docs/engineering/DOMAIN_MAP.json"))).domains;const domains=new Set(),effects=new Set();
for(const path of paths){for(const d of map)if([...(d.surfacePaths??[]),...(d.codeRoots??[]),...(d.serverBoundaries??[])].some(p=>path===p||path.startsWith(`${p}/`)||p.startsWith(`${path}/`)))domains.add(d.id);if(path.startsWith("supabase/migrations/"))effects.add("DATABASE");if(path.startsWith("src/app/api/"))effects.add("API");if(path.startsWith("src/app/")||path.startsWith("src/components/"))effects.add("UI");if(path==="src/lib/db.ts")effects.add("OFFLINE");if(/export|excel/i.test(path))effects.add("EXPORT");if(/^(scripts\/engineering|docs\/engineering|\.github|\.codex)/.test(path)){effects.add("ENGINEERING_CONTROL");domains.add("engineering-control");}}
if(/CREATE POLICY|DROP POLICY|ROW LEVEL SECURITY|SECURITY DEFINER|auth\.uid\(|GRANT|REVOKE/i.test(patch)){effects.add("AUTHORIZATION");effects.add("SECURITY");}const risk=["DATABASE","AUTHORIZATION","SECURITY","ENGINEERING_CONTROL"].some(x=>effects.has(x))?"R3":effects.size?"R2":"R0";console.log(JSON.stringify({baseSha:git("rev-parse",base),headSha:git("rev-parse",head),changedPaths:paths,domains:[...domains].sort(),effects:[...effects].sort(),risk,impactHash:createHash("sha256").update(patch).digest("hex")},null,2));
