import { spawnSync,execFileSync } from "node:child_process";import { readFileSync } from "node:fs";import { resolve } from "node:path";import { randomUUID } from "node:crypto";
const root=resolve(import.meta.dirname,"../.."),acceptanceFixture=JSON.stringify({localComplete:false,progressSignature:"fixture-progress",evidenceHash:"fixture-evidence",nextAction:{acceptanceId:"ZOS-A01",command:"npm run context:test"}}),run=(path,input)=>spawnSync("node",[resolve(root,path)],{cwd:resolve(root,"src"),input:JSON.stringify(input??{}),encoding:"utf8",env:{...process.env,ZEROGRAPH_ACCEPTANCE_FIXTURE:acceptanceFixture}}),parse=r=>r.stdout.trim()?JSON.parse(r.stdout):null,assert=(ok,code)=>{if(!ok)throw Error(code);};const config=JSON.parse(readFileSync(resolve(root,".codex/hooks.json"))),events=["SessionStart","UserPromptSubmit","PreToolUse","PostToolUse","Stop"];
const valid=events.every(e=>config.hooks[e]?.every(g=>Array.isArray(g.hooks)&&g.hooks.every(h=>h.type==="command"&&h.command&&h.commandWindows&&h.timeout)));assert(valid,"HOOK_CONFIG_INVALID");assert(!events.every(e=>({hooks:{UserPromptSubmit:[{command:"node old.mjs"}]}}).hooks[e]),"FLAT_HOOK_ACCEPTED");assert(JSON.stringify(config).includes("git rev-parse --show-toplevel"),"ROOT_RESOLUTION_MISSING");assert(JSON.stringify(config).includes("commandWindows"),"WINDOWS_COMMAND_MISSING");
const denied=parse(run("scripts/engineering/hooks/pre-tool.mjs",{tool_name:"Bash",tool_input:{command:"git reset --hard"}}));assert(denied?.hookSpecificOutput?.hookEventName==="PreToolUse"&&denied.hookSpecificOutput.permissionDecision==="deny"&&!denied.permissionDecision,"PRETOOL_WIRE_INVALID");assert(run("scripts/engineering/hooks/pre-tool.mjs",{tool_name:"Bash",tool_input:{command:"git status"}}).stdout==="","SAFE_COMMAND_OVERBLOCKED");assert(parse(run("scripts/engineering/hooks/pre-tool.mjs",{tool_name:"Write",tool_input:{path:"docs/engineering/OS_V3_ACCEPTANCE.json"}}))?.hookSpecificOutput?.permissionDecision==="deny","LOCK_NOT_PROTECTED");
const session_id=`bootstrap-${randomUUID()}`,owner="Owner mapping task",ownerOut=parse(run("scripts/engineering/hooks/user-prompt.mjs",{session_id,prompt:owner}));assert(ownerOut.hookSpecificOutput.hookEventName==="UserPromptSubmit","PROMPT_WIRE_INVALID");const sessionFile=execFileSync("git",["rev-parse","--git-path",`zerograph/sessions/${session_id}.json`],{cwd:root,encoding:"utf8"}).trim(),before=JSON.parse(readFileSync(resolve(root,sessionFile)));run("scripts/engineering/hooks/user-prompt.mjs",{session_id,prompt:"ZEROGRAPH_CONTINUE|acceptance=ZOS-A01"});const after=JSON.parse(readFileSync(resolve(root,sessionFile)));assert(before.originalTaskHash===after.originalTaskHash&&!JSON.stringify(after).includes(owner),"CONTINUATION_TASK_IDENTITY_LOST");const other=`bootstrap-${randomUUID()}`;run("scripts/engineering/hooks/user-prompt.mjs",{session_id:other,prompt:"Different task"});assert(sessionFile!==execFileSync("git",["rev-parse","--git-path",`zerograph/sessions/${other}.json`],{cwd:root,encoding:"utf8"}).trim(),"SESSION_ISOLATION_FAILED");
const s1=parse(run("scripts/engineering/hooks/stop.mjs",{session_id,stop_hook_active:false})),s2=parse(run("scripts/engineering/hooks/stop.mjs",{session_id,stop_hook_active:false})),s3=parse(run("scripts/engineering/hooks/stop.mjs",{session_id,stop_hook_active:false})),s4=parse(run("scripts/engineering/hooks/stop.mjs",{session_id,stop_hook_active:false}));assert(s1.decision==="block"&&s1.reason.startsWith("ZEROGRAPH_CONTINUE|"),"STOP_CONTINUATION_FAILED");assert(s2.reason.includes("strategy-change"),"STOP_STRATEGY_CHANGE_FAILED");assert(s3.reason.startsWith("ZEROGRAPH_STALL_REPORT|"),"STOP_STALL_REPORT_FAILED");assert(s4.stopReason==="STALL_LIMIT","STOP_LOOP_UNBOUNDED");assert(run("scripts/engineering/hooks/stop.mjs",{session_id,stop_hook_active:true}).stdout==="","STOP_ACTIVE_IGNORED");
const workflow=readFileSync(resolve(root,".github/workflows/product-verification.yml"),"utf8"),checkouts=(workflow.match(/uses: actions\/checkout@v4/g)??[]).length,refs=(workflow.match(/ref: \$\{\{ github\.event\.pull_request\.head\.sha \}\}/g)??[]).length;assert(checkouts===refs,"EXACT_HEAD_CHECKOUT_MISSING");assert(/receivables-postgres:[\s\S]*?if: always\(\)/.test(workflow)&&/e2e:[\s\S]*?if: always\(\)/.test(workflow)&&/verify:[\s\S]*?if: always\(\)/.test(workflow),"CI_EVIDENCE_DEADLOCK");assert(!/preflight:[\s\S]*?os:acceptance -- --mode release[\s\S]*?unit-build:/.test(workflow),"EARLY_RELEASE_GATE_PRESENT");console.log("ZERO_GRAPH_CONTROL_PLANE_V2_FIXTURES_PASS");
