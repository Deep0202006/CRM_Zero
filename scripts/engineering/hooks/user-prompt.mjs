import { execFileSync } from "node:child_process";import { loadState,readInput,repositoryState,root,saveState,sha } from "./state.mjs";
const input=await readInput(),session_id=input.session_id??"unknown",prompt=input.prompt??input.user_prompt??"",state=loadState(session_id),internal=/^(ZEROGRAPH_CONTINUE|ZEROGRAPH_STALL_REPORT)\|/.test(prompt);let context="ZeroGraph continuation; retain original Owner task identity.";
if(!internal){state.originalTaskHash=sha(prompt);try{const resolved=execFileSync("node",["scripts/engineering/context.mjs","--task",prompt],{cwd:root,encoding:"utf8"});const pack=JSON.parse(resolved);context=JSON.stringify({taskHash:state.originalTaskHash,domains:pack.domains,risk:pack.risk,authorities:pack.authorities,capabilities:pack.capabilities,mustNotWriteAuthorities:pack.mustNotWriteAuthorities,criticalTests:pack.criticalTests}).slice(0,3000);}catch{context=`taskHash=${state.originalTaskHash}; CONTEXT_AMBIGUOUS`;}}
saveState({...state,session_id,...repositoryState()});console.log(JSON.stringify({hookSpecificOutput:{hookEventName:"UserPromptSubmit",additionalContext:context}}));
