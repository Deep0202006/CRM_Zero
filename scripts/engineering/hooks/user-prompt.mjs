import { createHash } from "node:crypto"; import { mkdirSync, writeFileSync } from "node:fs"; import { execFileSync } from "node:child_process"; import { resolve } from "node:path";
const root=resolve(import.meta.dirname,"../../.."); let input=""; for await(const c of process.stdin) input+=c; const prompt=JSON.parse(input||"{}").prompt??""; const taskHash=createHash("sha256").update(prompt).digest("hex"); const dir=execFileSync("git",["rev-parse","--git-path","zerograph/sessions"],{cwd:root,encoding:"utf8"}).trim(); mkdirSync(resolve(root,dir),{recursive:true}); writeFileSync(resolve(root,dir,"current.json"),JSON.stringify({taskHash})); console.log(JSON.stringify({hookSpecificOutput:{hookEventName:"UserPromptSubmit",additionalContext:`ZeroGraph task ${taskHash.slice(0,12)}: resolve current source before writes.`}}));
