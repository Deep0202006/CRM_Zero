import { loadState,readInput,repositoryState,saveState,sha } from "./state.mjs";const input=await readInput(),session_id=input.session_id??"unknown",state=loadState(session_id),repo=repositoryState(),failure=JSON.stringify(input.tool_response??input.tool_result??"");saveState({...state,session_id,...repo,lastFailureSignature:failure?sha(failure):null});
