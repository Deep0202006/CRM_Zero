import { execFileSync } from "node:child_process"; import { mkdirSync, writeFileSync } from "node:fs"; import { resolve } from "node:path"; const root=resolve(import.meta.dirname,"../../.."); const dir=execFileSync("git",["rev-parse","--git-path","zerograph/sessions"],{cwd:root,encoding:"utf8"}).trim(); mkdirSync(resolve(root,dir),{recursive:true}); writeFileSync(resolve(root,dir,"tool.json"),JSON.stringify({head:execFileSync("git",["rev-parse","HEAD"],{cwd:root,encoding:"utf8"}).trim()}));
