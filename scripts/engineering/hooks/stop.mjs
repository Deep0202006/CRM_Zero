import { execFileSync } from "node:child_process"; import { resolve } from "node:path"; const root=resolve(import.meta.dirname,"../../.."); const out=execFileSync("node",["scripts/engineering/os-acceptance.mjs","--mode","stop"],{cwd:root,encoding:"utf8"}); const r=JSON.parse(out); if(!r.complete) console.log(JSON.stringify({decision:"block",reason:`IMPLEMENTATION_INCOMPLETE: ${r.nextAction} and exact next action` }));
