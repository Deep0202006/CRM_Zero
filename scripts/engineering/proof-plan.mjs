import { execFileSync } from "node:child_process";import { readFileSync } from "node:fs";import { resolve } from "node:path";import { createHash } from "node:crypto";const root=resolve(import.meta.dirname,"../.."),args=process.argv.slice(2),value=k=>args[args.indexOf(k)+1],base=value("--base")??"origin/main",head=value("--head")??"HEAD",run=(...a)=>execFileSync("git",a,{cwd:root,encoding:"utf8"}).trim(),impact=JSON.parse(execFileSync("node",["scripts/engineering/impact.mjs","--base",base,"--head",head],{cwd:root,encoding:"utf8"})),proofs=JSON.parse(readFileSync(resolve(root,"docs/engineering/PROOFS.json"))).proofs;
const applicable=p=>impact.domains.some(d=>(p.domains??[]).includes(d))&&impact.effects.some(e=>(p.effects??[]).includes(e));const required=proofs.filter(p=>impact.domains.includes("engineering-control")?p.controlPlaneCoverage===true:applicable(p));if(["DATABASE","AUTHORIZATION","SECURITY"].some(e=>impact.effects.includes(e))&&!required.some(p=>p.kind==="postgres")){console.error("R3_DB_PROOF_UNMAPPED");process.exit(2);}const plan={...impact,treeSha:run("rev-parse",`${head}^{tree}`),unitProofs:required.filter(p=>p.kind==="unit"),postgresProofs:required.filter(p=>p.kind==="postgres"),e2eProofs:required.filter(p=>p.kind==="e2e"),ownerProofs:required.filter(p=>p.runner==="owner-sql"),forbiddenWrites:["production"]};plan.planHash=createHash("sha256").update(JSON.stringify(plan)).digest("hex");console.log(JSON.stringify(plan,null,2));
