import { execFileSync } from "node:child_process"; import { readFileSync } from "node:fs"; import { resolve } from "node:path"; import { createHash } from "node:crypto";
const root=resolve(import.meta.dirname,"../.."); const args=process.argv.slice(2); const value=(key)=>args[args.indexOf(key)+1]; const base=value("--base")??"origin/main"; const head=value("--head")??"HEAD"; const impact=JSON.parse(execFileSync("node",["scripts/engineering/impact.mjs","--base",base,"--head",head],{cwd:root,encoding:"utf8"})); const proofs=JSON.parse(readFileSync(resolve(root,"docs/engineering/PROOFS.json"))).proofs; const required=proofs.filter(p=>impact.effects.some(e=>p.effects.includes(e))||impact.effects.includes("ENGINEERING_CONTROL")); if(["DATABASE","AUTHORIZATION","SECURITY"].some(e=>impact.effects.includes(e))&&!required.some(p=>p.kind==="postgres")){console.error("R3_DB_PROOF_UNMAPPED");process.exit(2);} const plan={...impact,treeSha:execFileSync("git",["rev-parse",`${head}^{tree}`],{cwd:root,encoding:"utf8"}).trim(),unitProofs:required.filter(p=>p.kind==="unit"),postgresProofs:required.filter(p=>p.kind==="postgres"),e2eProofs:required.filter(p=>p.kind==="e2e"),ownerProofs:required.filter(p=>p.runner==="owner-sql"),forbiddenWrites:["production"]}; plan.planHash=createHash("sha256").update(JSON.stringify(plan)).digest("hex"); console.log(JSON.stringify(plan,null,2));
