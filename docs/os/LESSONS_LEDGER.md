# Engineering Lessons Ledger

Record only genuinely new, reusable engineering knowledge from serious incidents. Do not include customer data, credentials, or verbose incident narratives.

| Date | Domain | Symptom | Wrong assumption / missing capability | Permanent lesson | Contract/guard/test/skill updated | Status |
| 2026-08-16 | Field Visits / Mapping | A new Visit field risked breaking historical/offline rows, while Mapping suggestions had become Pipeline validation and could materialize placeholder Leads. | UI labels, suggestion discovery, persistence authority, and cross-domain creation authority were conflated. | Label-only changes preserve storage authority; required-field upgrades keep legacy reads and previous queued fixtures; suggestion lists share one provider but remain non-authoritative; free-text logging preserves display values and has zero cross-domain writes; every field closes Write → Read → Admin → Export. | Field Visit and Mapping contracts, Golden Principles, pincode/mapping regression suites, SQL/harness guards. | Implemented in source; owner migration pending |
|---|---|---|---|---|---|---|
| 2026-08-13 | Distributor lifecycle | Renewals and operational status needed to appear within Payment Collections without becoming money or Lead authority. | Mutable Lead names and Receivables identity keys were treated as possible account identity, but neither can own an isolated lifecycle aggregate. | Use one stable operational account ID, optional cross-domain references, conservative ambiguous matching, orthogonal facts, derived IST reminders, expected-version writes, and declared read/side-effect budgets. Read the repo map, affected contract, skill, latest lessons, and active plan before inspecting only unresolved surfaces. | Distributor Status contract, authority/resource contracts, migration runtime suite, import/concurrency/isolation tests. | Implemented in source; owner migration pending |
| 2026-08-10 | Calls | Free-text clients stayed local and new calls waited behind old queue failures. | UI client references, offline repair, and server validation did not share one semantic contract; the outbox lacked exact-item priority confirmation. | UI client-reference semantics, offline payload semantics and server validation must share one canonical contract. Custom user-entered identities must never be silently interpreted as relational UUID identifiers. Critical new work should receive targeted confirmation through its existing durable outbox when backlog order is not a business dependency. | Calls contract and critical flow; client/sync/priority regression tests; Calls and Incident skills. A pattern guard was not suitable for this semantic rule. | Implemented |
| 2026-08-10 | Owner production safety | Production testing could be interpreted as permission to create and later delete dummy business records. | The harness prohibited fabricated records generally but did not state that cleanup does not make a live production test write safe. | Automated tests, QA fixtures, and smoke tests must never create dummy/test business data in live production Supabase. Production verification is read-only unless the owner explicitly authorizes the exact mutation. Use mocks, isolated fixtures, local/preview environments, and read-only reconciliation; never insert dummy data and then delete it. | Golden Principles, production-safety skill, release guidance, invariant guard, and harness self-test. | Permanent owner constraint |
| 2026-08-10 | Calls | Confirmed calls visible in Admin KPI could disappear from employee Call Logs. | Critical business history and optional derived metrics shared one all-or-nothing failure boundary; the local fallback did not expose degraded authority. | Critical business-history reads must not share an all-or-nothing failure boundary with optional derived metrics. Metric failure may degrade metrics but must never suppress successfully retrieved authoritative records. | Calls contract, `callHistoryAuthority` regression suite, and incident note. Regex guard declined as semantically brittle. | Implemented |
| 2026-08-10 | Pipeline | Offline or failed stage transitions can later overwrite newer server state. | A business transition was treated as a generic row update, so the durable outbox lost its expected-stage precondition and replay disabled concurrency checks. | Durable state-machine work must preserve the command’s identity, expected state, target state, actor, and conflict outcome. A generic row patch is not a safe retry contract for a business transition. | Pipeline contract, semantic outbox, server-boundary migration, UI state, and regression tests. | Implemented; migration applied |
| 2026-08-10 | Engineering OS / Pipeline | Pipeline verification omitted the real employee board. | The domain map referenced stale `src/app/pipeline/` while production UI lived at `src/app/onboarding/`. | Domain automation must map the actual production route/code surface; stale path mappings can let critical UI logic escape domain verification. | Pipeline harness paths/tests and concise `zd-pipeline` skill. | Implemented |
| 2026-08-10 | Pipeline | Server-authoritative boards made some previously advanced browser-local stages appear reset. | Promoting authority was treated as sufficient convergence even though legitimate durable local business intent predated the semantic command contract. | When promoting a local-first workflow to server authority, release planning must explicitly reconcile preserved local business state. Server authority prevents future split-brain but does not automatically resolve legitimate unsynced historical intent. | Pipeline contract, recovery planner/passive-evidence tests, incident note, and Pipeline skill. | Implemented; replay side effects gated |
| 2026-08-10 | Calls | Employees saw the recent page size as their lifetime call count. | Loaded-page length and the server-authoritative business total were treated as the same contract. | Pagination window size and business total are different contracts. A displayed server-authoritative history total must come from authoritative count metadata, never the currently loaded array length. | Calls contract, Calls skill, repository/UI and regression tests. | Implemented |
| 2026-08-11 | Financial batch mutations | An import function could insert early rows and then return an error for a later row. | Returning an error value from PL/pgSQL was mistaken for transaction rollback. | Atomic batch means complete write-free validation before mutation, or an exception that aborts the transaction. Never convert a post-write failure into an ordinary successful function return. | Receivables two-phase import function, late-row rollback PostgreSQL tests, contract and skill. | Implemented |
| 2026-08-11 | R3 schema verification | Static migration pattern tests could pass without proving PostgreSQL accepts or enforces the SQL. | Text inspection was treated as sufficient migration evidence. | Financial/schema-critical migrations require disposable runtime database apply and invariant/RLS tests; static guards remain supplementary only. | Pinned PostgreSQL CI service, migration integration suite, Receivables contract and skill. | Implemented |
| 2026-08-11 | Durable command recovery | A deterministic unique constraint could surface as HTTP 503 and remain in an automatic retry outbox forever. | Retry classification followed the transport wrapper instead of semantic certainty. | Expected database constraint failures must become typed terminal results after a rollback-safe subtransaction; only genuinely uncertain infrastructure outcomes belong in automatic retry. | Receivables command RPC/API, PostgreSQL duplicate tests, contract and skill. | Implemented |
| 2026-08-11 | Financial bulk intake | A bounded 5,000-row function was transactionally correct but repeatedly concatenated growing JSONB state. | A row cap was mistaken for acceptable algorithmic complexity. | Financial batch validation must use indexed/set-based transaction-local staging; bounded input does not excuse quadratic accumulation. | Receivables migration 035, 5,000-row PostgreSQL success/late-failure tests, contract and skill. | Implemented in source; owner migration pending |
| 2026-08-11 | Financial export | User-controlled spreadsheet cells could begin with formula operators. | XLSX generation was treated as inert serialization. | Spreadsheet exports must neutralize formula-capable user text before workbook generation and test the serialized cell values. | Receivables export helper, workbook regression tests, contract and skill. | Implemented in source |
| 2026-08-12 | Evidence lifecycle | Permanent business metadata and temporary selfie bytes had no separate lifecycle; attendance also embedded some evidence directly in database rows. | Evidence retention was treated as row retention instead of an exact object lifecycle with durable state. | Temporary media expiry must begin after successful upload, delete only an authoritative exact Storage key, preserve its business row, expose a terminal purged state, and retry partial failures idempotently. | Data Lifecycle and Field Visits contracts, retention planner/cron tests, admin evidence state, and Field Visits skill. | Implemented in source; owner migration pending |
