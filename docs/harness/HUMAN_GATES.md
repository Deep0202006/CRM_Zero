# Human gates

Stop for production SQL, Supabase execution, destructive migrations, RLS broadening, service-role use, credential changes, production deploys, attribution-changing backfills, restore operations, ambiguous recovery, major dependency upgrades, scope expansion, force push, or merge to main. Record approval outside generated evidence; the harness does not grant authority.
