# Adding an area

Copy an existing capsule in `harness/areas/`, choose a unique name, and fill every schema field. Keep invariants short and reference stable docs. Map every production entrypoint to required tests and narrowly allowed paths. No script registry change is needed.
