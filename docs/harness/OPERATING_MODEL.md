# Operating model

One orchestrator owns each change campaign. Start with the generated context, declare exceptions in task state, edit only approved paths, and use quick checks during work. Full verification produces evidence before commit; release mode adds scope, audit review, and both handoffs. The harness never applies SQL, deploys, merges, or crosses a human gate.
